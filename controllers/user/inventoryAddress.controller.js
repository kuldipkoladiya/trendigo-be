/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { inventoryAddressService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getInventoryAddress = catchAsync(async (req, res) => {
  const { inventoryAddressId } = req.params;
  const filter = {
    _id: inventoryAddressId,
  };
  const options = {};
  const inventoryAddress = await inventoryAddressService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: inventoryAddress });
});

export const listInventoryAddress = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const inventoryAddress = await inventoryAddressService.getInventoryAddressList(filter, options);
  return res.status(httpStatus.OK).send({ results: inventoryAddress });
});

export const paginateInventoryAddress = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const inventoryAddress = await inventoryAddressService.getInventoryAddressListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: inventoryAddress });
});

export const createInventoryAddress = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const inventoryAddress = await inventoryAddressService.createInventoryAddress(body, options);
  return res.status(httpStatus.CREATED).send({ results: inventoryAddress });
});

export const updateInventoryAddress = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { inventoryAddressId } = req.params;
  const filter = {
    _id: inventoryAddressId,
  };
  const options = { new: true };
  const inventoryAddress = await inventoryAddressService.updateInventoryAddress(filter, body, options);
  return res.status(httpStatus.OK).send({ results: inventoryAddress });
});

export const removeInventoryAddress = catchAsync(async (req, res) => {
  const { inventoryAddressId } = req.params;
  const filter = {
    _id: inventoryAddressId,
  };
  const inventoryAddress = await inventoryAddressService.removeInventoryAddress(filter);
  return res.status(httpStatus.OK).send({ results: inventoryAddress });
});
