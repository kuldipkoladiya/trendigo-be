/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { userlikeService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getUserlike = catchAsync(async (req, res) => {
  const { userlikeId } = req.params;
  const filter = {
    _id: userlikeId,
  };
  const options = {};
  const userlike = await userlikeService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: userlike });
});

export const listUserlike = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const userlike = await userlikeService.getUserlikeList(filter, options);
  return res.status(httpStatus.OK).send({ results: userlike });
});

export const paginateUserlike = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const userlike = await userlikeService.getUserlikeListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: userlike });
});

export const createUserlike = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const userlike = await userlikeService.createUserlike(body, options);
  return res.status(httpStatus.CREATED).send({ results: userlike });
});

export const updateUserlike = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { userlikeId } = req.params;
  const filter = {
    _id: userlikeId,
  };
  const options = { new: true };
  const userlike = await userlikeService.updateUserlike(filter, body, options);
  return res.status(httpStatus.OK).send({ results: userlike });
});

export const removeUserlike = catchAsync(async (req, res) => {
  const { userlikeId } = req.params;
  const filter = {
    _id: userlikeId,
  };
  const userlike = await userlikeService.removeUserlike(filter);
  return res.status(httpStatus.OK).send({ results: userlike });
});
