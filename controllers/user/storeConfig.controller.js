/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { s3Service, storeConfigService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import FileFieldValidationEnum from 'models/fileFieldValidation.model';
import mongoose from 'mongoose';
import TempS3 from 'models/tempS3.model';
import { asyncForEach } from 'utils/common';
import config from 'config/config';
import { pick } from '../../utils/pick';

const moveFileAndUpdateTempS3 = async ({ url, newFilePath }) => {
  const newUrl = await s3Service.moveFile({ key: url, newFilePath });
  await TempS3.findOneAndUpdate({ url }, { url: newUrl });
  return newUrl;
};
// this is used to move file to new specified path as shown in basePath, used in create and update controller.
const moveFiles = async ({ body, user, moveFileObj }) => {
  await asyncForEach(Object.keys(moveFileObj), async (key) => {
    const fieldValidation = FileFieldValidationEnum[`${key}OfStoreConfig`];
    const basePath = `users/${user._id}/storeConfig/${body._id}/${key}/${mongoose.Types.ObjectId()}/`;
    if (Array.isArray(moveFileObj[key])) {
      const newUrlsArray = [];
      moveFileObj[key].map(async (ele) => {
        const filePath = `${mongoose.Types.ObjectId()}_${ele.split('/').pop()}`;
        newUrlsArray.push(moveFileAndUpdateTempS3({ url: ele, newFilePath: basePath + filePath }));
      });
      Object.assign(body, { ...body, [key]: await Promise.all(newUrlsArray) });
    } else {
      const filePath = `${mongoose.Types.ObjectId()}_${moveFileObj[key].split('/').pop()}`;
      Object.assign(body, {
        ...body,
        [key]: await moveFileAndUpdateTempS3({
          url: moveFileObj[key],
          newFilePath: basePath + filePath,
        }),
      });
      if (fieldValidation.generateThumbnails) {
        Object.assign(body, {
          ...body,
          [`thumbOf${key}`]: await s3Service.createThumbnails({
            url: moveFileObj[key],
            resolutions: fieldValidation.thumbnailResolutions,
          }),
        });
      }
    }
  });
};
export const getStoreConfig = catchAsync(async (req, res) => {
  const { storeConfigId } = req.params;
  const filter = {
    _id: storeConfigId,
  };
  const options = {};
  const storeConfig = await storeConfigService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: storeConfig });
});

export const listStoreConfig = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const storeConfig = await storeConfigService.getStoreConfigList(filter, options);
  return res.status(httpStatus.OK).send({ results: storeConfig });
});

export const paginateStoreConfig = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const storeConfig = await storeConfigService.getStoreConfigListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: storeConfig });
});

export const createStoreConfig = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const { user } = req;
  const moveFileObj = {
    ...(body.banner && { banner: body.banner }),
  };
  body._id = mongoose.Types.ObjectId();
  await moveFiles({ body, user, moveFileObj });
  const options = {};
  const storeConfigResult = await storeConfigService.createStoreConfig(body, options);
  if (storeConfigResult) {
    const uploadedFileUrls = [];
    uploadedFileUrls.push(storeConfigResult.banner);
    await TempS3.updateMany({ url: { $in: uploadedFileUrls } }, { active: true });
  }
  return res.status(httpStatus.CREATED).send({ results: storeConfigResult });
});

export const updateStoreConfig = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { storeConfigId } = req.params;
  const { user } = req;
  const moveFileObj = {
    ...(body.banner && { banner: body.banner }),
  };
  body._id = storeConfigId;
  await moveFiles({ body, user, moveFileObj });
  const filter = {
    _id: storeConfigId,
  };
  const options = { new: true };
  const storeConfigResult = await storeConfigService.updateStoreConfig(filter, body, options);
  // tempS3
  if (storeConfigResult) {
    const uploadedFileUrls = [];
    uploadedFileUrls.push(storeConfigResult.banner);
    await TempS3.updateMany({ url: { $in: uploadedFileUrls } }, { active: true });
  }
  return res.status(httpStatus.OK).send({ results: storeConfigResult });
});

export const removeStoreConfig = catchAsync(async (req, res) => {
  const { storeConfigId } = req.params;
  const filter = {
    _id: storeConfigId,
  };
  const storeConfig = await storeConfigService.removeStoreConfig(filter);
  return res.status(httpStatus.OK).send({ results: storeConfig });
});
