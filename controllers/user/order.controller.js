/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { orderService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getOrder = catchAsync(async (req, res) => {
  const { orderId } = req.params;
  const filter = {
    _id: orderId,
  };
  const options = {};
  const order = await orderService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: order });
});

export const listOrder = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const order = await orderService.getOrderList(filter, options);
  return res.status(httpStatus.OK).send({ results: order });
});

export const paginateOrder = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const order = await orderService.getOrderListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: order });
});

export const createOrder = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const order = await orderService.createOrder(body, options);
  return res.status(httpStatus.CREATED).send({ results: order });
});

export const updateOrder = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { orderId } = req.params;
  const filter = {
    _id: orderId,
  };
  const options = { new: true };
  const order = await orderService.updateOrder(filter, body, options);
  return res.status(httpStatus.OK).send({ results: order });
});

export const removeOrder = catchAsync(async (req, res) => {
  const { orderId } = req.params;
  const filter = {
    _id: orderId,
  };
  const order = await orderService.removeOrder(filter);
  return res.status(httpStatus.OK).send({ results: order });
});
