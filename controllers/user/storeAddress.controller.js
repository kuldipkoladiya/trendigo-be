/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { storeAddressService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getStoreAddress = catchAsync(async (req, res) => {
  const { storeAddressId } = req.params;
  const filter = {
    _id: storeAddressId,
  };
  const options = {};
  const storeAddress = await storeAddressService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: storeAddress });
});

export const listStoreAddress = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const storeAddress = await storeAddressService.getStoreAddressList(filter, options);
  return res.status(httpStatus.OK).send({ results: storeAddress });
});

export const paginateStoreAddress = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const storeAddress = await storeAddressService.getStoreAddressListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: storeAddress });
});

export const createStoreAddress = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const storeAddress = await storeAddressService.createStoreAddress(body, options);
  return res.status(httpStatus.CREATED).send({ results: storeAddress });
});

export const updateStoreAddress = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { storeAddressId } = req.params;
  const filter = {
    _id: storeAddressId,
  };
  const options = { new: true };
  const storeAddress = await storeAddressService.updateStoreAddress(filter, body, options);
  return res.status(httpStatus.OK).send({ results: storeAddress });
});

export const removeStoreAddress = catchAsync(async (req, res) => {
  const { storeAddressId } = req.params;
  const filter = {
    _id: storeAddressId,
  };
  const storeAddress = await storeAddressService.removeStoreAddress(filter);
  return res.status(httpStatus.OK).send({ results: storeAddress });
});
