/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { userCoupanService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getUserCoupan = catchAsync(async (req, res) => {
  const { userCoupanId } = req.params;
  const filter = {
    _id: userCoupanId,
  };
  const options = {};
  const userCoupan = await userCoupanService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: userCoupan });
});

export const listUserCoupan = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const userCoupan = await userCoupanService.getUserCoupanList(filter, options);
  return res.status(httpStatus.OK).send({ results: userCoupan });
});

export const paginateUserCoupan = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const userCoupan = await userCoupanService.getUserCoupanListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: userCoupan });
});

export const createUserCoupan = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const userCoupan = await userCoupanService.createUserCoupan(body, options);
  return res.status(httpStatus.CREATED).send({ results: userCoupan });
});

export const updateUserCoupan = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { userCoupanId } = req.params;
  const filter = {
    _id: userCoupanId,
  };
  const options = { new: true };
  const userCoupan = await userCoupanService.updateUserCoupan(filter, body, options);
  return res.status(httpStatus.OK).send({ results: userCoupan });
});

export const removeUserCoupan = catchAsync(async (req, res) => {
  const { userCoupanId } = req.params;
  const filter = {
    _id: userCoupanId,
  };
  const userCoupan = await userCoupanService.removeUserCoupan(filter);
  return res.status(httpStatus.OK).send({ results: userCoupan });
});
