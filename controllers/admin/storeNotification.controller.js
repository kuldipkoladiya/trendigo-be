/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { storeNotificationService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getStoreNotification = catchAsync(async (req, res) => {
  const { storeNotificationId } = req.params;
  const filter = {
    _id: storeNotificationId,
  };
  const options = {};
  const storeNotification = await storeNotificationService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: storeNotification });
});

export const listStoreNotification = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const storeNotification = await storeNotificationService.getStoreNotificationList(filter, options);
  return res.status(httpStatus.OK).send({ results: storeNotification });
});

export const paginateStoreNotification = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const storeNotification = await storeNotificationService.getStoreNotificationListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: storeNotification });
});

export const createStoreNotification = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const storeNotification = await storeNotificationService.createStoreNotification(body, options);
  return res.status(httpStatus.CREATED).send({ results: storeNotification });
});

export const updateStoreNotification = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { storeNotificationId } = req.params;
  const filter = {
    _id: storeNotificationId,
  };
  const options = { new: true };
  const storeNotification = await storeNotificationService.updateStoreNotification(filter, body, options);
  return res.status(httpStatus.OK).send({ results: storeNotification });
});

export const removeStoreNotification = catchAsync(async (req, res) => {
  const { storeNotificationId } = req.params;
  const filter = {
    _id: storeNotificationId,
  };
  const storeNotification = await storeNotificationService.removeStoreNotification(filter);
  return res.status(httpStatus.OK).send({ results: storeNotification });
});
