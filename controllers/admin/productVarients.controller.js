/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { productVarientsService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getProductVarients = catchAsync(async (req, res) => {
  const { productVarientsId } = req.params;
  const filter = {
    _id: productVarientsId,
  };
  const options = {};
  const productVarients = await productVarientsService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: productVarients });
});

export const listProductVarients = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const productVarients = await productVarientsService.getProductVarientsList(filter, options);
  return res.status(httpStatus.OK).send({ results: productVarients });
});

export const paginateProductVarients = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const productVarients = await productVarientsService.getProductVarientsListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: productVarients });
});

export const createProductVarients = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const productVarients = await productVarientsService.createProductVarients(body, options);
  return res.status(httpStatus.CREATED).send({ results: productVarients });
});

export const updateProductVarients = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { productVarientsId } = req.params;
  const filter = {
    _id: productVarientsId,
  };
  const options = { new: true };
  const productVarients = await productVarientsService.updateProductVarients(filter, body, options);
  return res.status(httpStatus.OK).send({ results: productVarients });
});

export const removeProductVarients = catchAsync(async (req, res) => {
  const { productVarientsId } = req.params;
  const filter = {
    _id: productVarientsId,
  };
  const productVarients = await productVarientsService.removeProductVarients(filter);
  return res.status(httpStatus.OK).send({ results: productVarients });
});
