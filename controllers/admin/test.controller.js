/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { testService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getTest = catchAsync(async (req, res) => {
  const { testId } = req.params;
  const filter = {
    _id: testId,
  };
  const options = {};
  const test = await testService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: test });
});

export const listTest = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const test = await testService.getTestList(filter, options);
  return res.status(httpStatus.OK).send({ results: test });
});

export const paginateTest = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const test = await testService.getTestListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: test });
});

export const createTest = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const test = await testService.createTest(body, options);
  return res.status(httpStatus.CREATED).send({ results: test });
});

export const updateTest = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { testId } = req.params;
  const filter = {
    _id: testId,
  };
  const options = { new: true };
  const test = await testService.updateTest(filter, body, options);
  return res.status(httpStatus.OK).send({ results: test });
});

export const removeTest = catchAsync(async (req, res) => {
  const { testId } = req.params;
  const filter = {
    _id: testId,
  };
  const test = await testService.removeTest(filter);
  return res.status(httpStatus.OK).send({ results: test });
});
