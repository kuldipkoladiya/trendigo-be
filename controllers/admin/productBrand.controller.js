/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { productBrandService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getProductBrand = catchAsync(async (req, res) => {
  const { productBrandId } = req.params;
  const filter = {
    _id: productBrandId,
  };
  const options = {};
  const productBrand = await productBrandService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: productBrand });
});

export const listProductBrand = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const productBrand = await productBrandService.getProductBrandList(filter, options);
  return res.status(httpStatus.OK).send({ results: productBrand });
});

export const paginateProductBrand = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const productBrand = await productBrandService.getProductBrandListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: productBrand });
});

export const createProductBrand = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const productBrand = await productBrandService.createProductBrand(body, options);
  return res.status(httpStatus.CREATED).send({ results: productBrand });
});

export const updateProductBrand = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { productBrandId } = req.params;
  const filter = {
    _id: productBrandId,
  };
  const options = { new: true };
  const productBrand = await productBrandService.updateProductBrand(filter, body, options);
  return res.status(httpStatus.OK).send({ results: productBrand });
});

export const removeProductBrand = catchAsync(async (req, res) => {
  const { productBrandId } = req.params;
  const filter = {
    _id: productBrandId,
  };
  const productBrand = await productBrandService.removeProductBrand(filter);
  return res.status(httpStatus.OK).send({ results: productBrand });
});
