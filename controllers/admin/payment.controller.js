/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { paymentService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getPayment = catchAsync(async (req, res) => {
  const { paymentId } = req.params;
  const filter = {
    _id: paymentId,
  };
  const options = {};
  const payment = await paymentService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: payment });
});

export const listPayment = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const payment = await paymentService.getPaymentList(filter, options);
  return res.status(httpStatus.OK).send({ results: payment });
});

export const paginatePayment = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const payment = await paymentService.getPaymentListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: payment });
});

export const createPayment = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const payment = await paymentService.createPayment(body, options);
  return res.status(httpStatus.CREATED).send({ results: payment });
});

export const updatePayment = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { paymentId } = req.params;
  const filter = {
    _id: paymentId,
  };
  const options = { new: true };
  const payment = await paymentService.updatePayment(filter, body, options);
  return res.status(httpStatus.OK).send({ results: payment });
});

export const removePayment = catchAsync(async (req, res) => {
  const { paymentId } = req.params;
  const filter = {
    _id: paymentId,
  };
  const payment = await paymentService.removePayment(filter);
  return res.status(httpStatus.OK).send({ results: payment });
});
