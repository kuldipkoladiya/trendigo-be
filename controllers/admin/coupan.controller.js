/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { coupanService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getCoupan = catchAsync(async (req, res) => {
  const { coupanId } = req.params;
  const filter = {
    _id: coupanId,
  };
  const options = {};
  const coupan = await coupanService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: coupan });
});

export const listCoupan = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const coupan = await coupanService.getCoupanList(filter, options);
  return res.status(httpStatus.OK).send({ results: coupan });
});

export const paginateCoupan = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const coupan = await coupanService.getCoupanListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: coupan });
});

export const createCoupan = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const coupan = await coupanService.createCoupan(body, options);
  return res.status(httpStatus.CREATED).send({ results: coupan });
});

export const updateCoupan = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { coupanId } = req.params;
  const filter = {
    _id: coupanId,
  };
  const options = { new: true };
  const coupan = await coupanService.updateCoupan(filter, body, options);
  return res.status(httpStatus.OK).send({ results: coupan });
});

export const removeCoupan = catchAsync(async (req, res) => {
  const { coupanId } = req.params;
  const filter = {
    _id: coupanId,
  };
  const coupan = await coupanService.removeCoupan(filter);
  return res.status(httpStatus.OK).send({ results: coupan });
});
