/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { businessCategoryService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getBusinessCategory = catchAsync(async (req, res) => {
  const { businessCategoryId } = req.params;
  const filter = {
    _id: businessCategoryId,
  };
  const options = {};
  const businessCategory = await businessCategoryService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: businessCategory });
});

export const listBusinessCategory = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const businessCategory = await businessCategoryService.getBusinessCategoryList(filter, options);
  return res.status(httpStatus.OK).send({ results: businessCategory });
});

export const paginateBusinessCategory = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const businessCategory = await businessCategoryService.getBusinessCategoryListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: businessCategory });
});

export const createBusinessCategory = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const businessCategory = await businessCategoryService.createBusinessCategory(body, options);
  return res.status(httpStatus.CREATED).send({ results: businessCategory });
});

export const updateBusinessCategory = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { businessCategoryId } = req.params;
  const filter = {
    _id: businessCategoryId,
  };
  const options = { new: true };
  const businessCategory = await businessCategoryService.updateBusinessCategory(filter, body, options);
  return res.status(httpStatus.OK).send({ results: businessCategory });
});

export const removeBusinessCategory = catchAsync(async (req, res) => {
  const { businessCategoryId } = req.params;
  const filter = {
    _id: businessCategoryId,
  };
  const businessCategory = await businessCategoryService.removeBusinessCategory(filter);
  return res.status(httpStatus.OK).send({ results: businessCategory });
});
