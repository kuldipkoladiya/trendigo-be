/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import httpStatus from 'http-status';
import { pickupAddressService } from 'services';
import { catchAsync } from 'utils/catchAsync';
import { pick } from '../../utils/pick';

export const getPickupAddress = catchAsync(async (req, res) => {
  const { pickupAddressId } = req.params;
  const filter = {
    _id: pickupAddressId,
  };
  const options = {};
  const pickupAddress = await pickupAddressService.getOne(filter, options);
  return res.status(httpStatus.OK).send({ results: pickupAddress });
});

export const listPickupAddress = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const pickupAddress = await pickupAddressService.getPickupAddressList(filter, options);
  return res.status(httpStatus.OK).send({ results: pickupAddress });
});

export const paginatePickupAddress = catchAsync(async (req, res) => {
  const filter = {};
  const options = {};
  const pickupAddress = await pickupAddressService.getPickupAddressListWithPagination(filter, options);
  return res.status(httpStatus.OK).send({ results: pickupAddress });
});

export const createPickupAddress = catchAsync(async (req, res) => {
  const { body } = req;
  body.createdBy = req.user._id;
  body.updatedBy = req.user._id;
  const options = {};
  const pickupAddress = await pickupAddressService.createPickupAddress(body, options);
  return res.status(httpStatus.CREATED).send({ results: pickupAddress });
});

export const updatePickupAddress = catchAsync(async (req, res) => {
  const { body } = req;
  body.updatedBy = req.user;
  const { pickupAddressId } = req.params;
  const filter = {
    _id: pickupAddressId,
  };
  const options = { new: true };
  const pickupAddress = await pickupAddressService.updatePickupAddress(filter, body, options);
  return res.status(httpStatus.OK).send({ results: pickupAddress });
});

export const removePickupAddress = catchAsync(async (req, res) => {
  const { pickupAddressId } = req.params;
  const filter = {
    _id: pickupAddressId,
  };
  const pickupAddress = await pickupAddressService.removePickupAddress(filter);
  return res.status(httpStatus.OK).send({ results: pickupAddress });
});
