/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import enumFields from 'models/enum.model';

Joi.objectId = require('joi-objectid')(Joi);

export const createStoreNotification = {
  body: Joi.object().keys({
    storeId: Joi.objectId(),
    isSmsOn: Joi.bool(),
    performanceReport: Joi.bool(),
    emailNotification: Joi.bool(),
    orderUpdates: Joi.bool(),
    paymentUpdate: Joi.bool(),
    lowStockAlerts: Joi.bool(),
    performanceReportTime: Joi.string().valid(...Object.values(enumFields.EnumPerformanceReportTimeOfStoreNotification)),
  }),
};

export const updateStoreNotification = {
  body: Joi.object().keys({
    storeId: Joi.objectId(),
    isSmsOn: Joi.bool(),
    performanceReport: Joi.bool(),
    emailNotification: Joi.bool(),
    orderUpdates: Joi.bool(),
    paymentUpdate: Joi.bool(),
    lowStockAlerts: Joi.bool(),
    performanceReportTime: Joi.string().valid(...Object.values(enumFields.EnumPerformanceReportTimeOfStoreNotification)),
  }),
  params: Joi.object().keys({
    storeNotificationId: Joi.objectId().required(),
  }),
};

export const getStoreNotificationById = {
  params: Joi.object().keys({
    storeNotificationId: Joi.objectId().required(),
  }),
};

export const deleteStoreNotificationById = {
  params: Joi.object().keys({
    storeNotificationId: Joi.objectId().required(),
  }),
};

export const getStoreNotification = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedStoreNotification = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
