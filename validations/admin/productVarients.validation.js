/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createProductVarients = {
  body: Joi.object().keys({
    productCategoryId: Joi.objectId(),
    key: Joi.string(),
    value: Joi.object().unknown(true),
  }),
};

export const updateProductVarients = {
  body: Joi.object().keys({
    productCategoryId: Joi.objectId(),
    key: Joi.string(),
    value: Joi.object().unknown(true),
  }),
  params: Joi.object().keys({
    productVarientsId: Joi.objectId().required(),
  }),
};

export const getProductVarientsById = {
  params: Joi.object().keys({
    productVarientsId: Joi.objectId().required(),
  }),
};

export const deleteProductVarientsById = {
  params: Joi.object().keys({
    productVarientsId: Joi.objectId().required(),
  }),
};

export const getProductVarients = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedProductVarients = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
