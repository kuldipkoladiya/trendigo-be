/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createCoupan = {
  body: Joi.object().keys({
    isActive: Joi.bool(),
    minOrderValue: Joi.number().integer(),
    maxCoupanAmount: Joi.number().integer(),
    storeId: Joi.objectId(),
    product: Joi.array().items(Joi.objectId()),
    coupanCode: Joi.array().items(Joi.string()),
  }),
};

export const updateCoupan = {
  body: Joi.object().keys({
    isActive: Joi.bool(),
    minOrderValue: Joi.number().integer(),
    maxCoupanAmount: Joi.number().integer(),
    storeId: Joi.objectId(),
    product: Joi.array().items(Joi.objectId()),
    coupanCode: Joi.array().items(Joi.string()),
  }),
  params: Joi.object().keys({
    coupanId: Joi.objectId().required(),
  }),
};

export const getCoupanById = {
  params: Joi.object().keys({
    coupanId: Joi.objectId().required(),
  }),
};

export const deleteCoupanById = {
  params: Joi.object().keys({
    coupanId: Joi.objectId().required(),
  }),
};

export const getCoupan = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedCoupan = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
