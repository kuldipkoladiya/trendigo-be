/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createBankDetails = {
  body: Joi.object().keys({
    storeId: Joi.objectId(),
    accountNumber: Joi.number().integer().required(),
    ifscCode: Joi.string().required(),
    isDefault: Joi.bool(),
    isVerified: Joi.bool(),
  }),
};

export const updateBankDetails = {
  body: Joi.object().keys({
    storeId: Joi.objectId(),
    accountNumber: Joi.number().integer(),
    ifscCode: Joi.string(),
    isDefault: Joi.bool(),
    isVerified: Joi.bool(),
  }),
  params: Joi.object().keys({
    bankDetailsId: Joi.objectId().required(),
  }),
};

export const getBankDetailsById = {
  params: Joi.object().keys({
    bankDetailsId: Joi.objectId().required(),
  }),
};

export const deleteBankDetailsById = {
  params: Joi.object().keys({
    bankDetailsId: Joi.objectId().required(),
  }),
};

export const getBankDetails = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedBankDetails = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
