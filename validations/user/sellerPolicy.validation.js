/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

const otherEmbed = Joi.object().keys({
  key: Joi.string(),
  value: Joi.string(),
});
export const createSellerPolicy = {
  body: Joi.object().keys({
    store: Joi.objectId(),
    privacy: Joi.array().items(Joi.string()),
    returnPolicy: Joi.array().items(Joi.string()),
    shipping: Joi.array().items(Joi.string()),
    other: otherEmbed,
  }),
};

export const updateSellerPolicy = {
  body: Joi.object().keys({
    store: Joi.objectId(),
    privacy: Joi.array().items(Joi.string()),
    returnPolicy: Joi.array().items(Joi.string()),
    shipping: Joi.array().items(Joi.string()),
    other: otherEmbed,
  }),
  params: Joi.object().keys({
    sellerPolicyId: Joi.objectId().required(),
  }),
};

export const getSellerPolicyById = {
  params: Joi.object().keys({
    sellerPolicyId: Joi.objectId().required(),
  }),
};

export const deleteSellerPolicyById = {
  params: Joi.object().keys({
    sellerPolicyId: Joi.objectId().required(),
  }),
};

export const getSellerPolicy = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedSellerPolicy = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};

export const getSellerPolicyByStoreId = {
  params: Joi.object().keys({
    storeId: Joi.objectId().required(),
  }),
};
