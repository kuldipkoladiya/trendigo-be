/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';
import config from 'config/config';

Joi.objectId = require('joi-objectid')(Joi);

export const createUserDashboardConfig = {
  body: Joi.object().keys({
    banner: Joi.string().regex(
      new RegExp(
        `https://${config.aws.bucket}.s3.${config.aws.region}.amazonaws.com\\b([-a-zA-Z0-9()@:%_+.~#?&amp;/=]*.(png)$)`
      )
    ),
  }),
};

export const updateUserDashboardConfig = {
  body: Joi.object().keys({
    banner: Joi.string().regex(
      new RegExp(
        `https://${config.aws.bucket}.s3.${config.aws.region}.amazonaws.com\\b([-a-zA-Z0-9()@:%_+.~#?&amp;/=]*.(png)$)`
      )
    ),
  }),
  params: Joi.object().keys({
    userDashboardConfigId: Joi.objectId().required(),
  }),
};

export const getUserDashboardConfigById = {
  params: Joi.object().keys({
    userDashboardConfigId: Joi.objectId().required(),
  }),
};

export const deleteUserDashboardConfigById = {
  params: Joi.object().keys({
    userDashboardConfigId: Joi.objectId().required(),
  }),
};

export const getUserDashboardConfig = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedUserDashboardConfig = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
