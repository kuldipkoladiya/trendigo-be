/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createStoreAddress = {
  body: Joi.object().keys({
    storeId: Joi.objectId(),
    pincode: Joi.number().integer().required(),
    city: Joi.string().required(),
    country: Joi.string().required(),
    state: Joi.string().required(),
    AddressLineOne: Joi.string().required(),
    addessLineTwo: Joi.string(),
  }),
};

export const updateStoreAddress = {
  body: Joi.object().keys({
    storeId: Joi.objectId(),
    pincode: Joi.number().integer(),
    city: Joi.string(),
    country: Joi.string(),
    state: Joi.string(),
    AddressLineOne: Joi.string(),
    addessLineTwo: Joi.string(),
  }),
  params: Joi.object().keys({
    storeAddressId: Joi.objectId().required(),
  }),
};

export const getStoreAddressById = {
  params: Joi.object().keys({
    storeAddressId: Joi.objectId().required(),
  }),
};

export const deleteStoreAddressById = {
  params: Joi.object().keys({
    storeAddressId: Joi.objectId().required(),
  }),
};

export const getStoreAddress = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedStoreAddress = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
