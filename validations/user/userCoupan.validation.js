/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createUserCoupan = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    coupanValue: Joi.number().integer(),
    minOrderValue: Joi.number().integer(),
    maxCoupanAmount: Joi.number().integer(),
    coupenPercentage: Joi.number().integer(),
    coupanCode: Joi.string(),
    productId: Joi.array().items(Joi.objectId()),
    storeId: Joi.array().items(Joi.objectId()),
  }),
};

export const updateUserCoupan = {
  body: Joi.object().keys({
    userId: Joi.objectId(),
    coupanValue: Joi.number().integer(),
    minOrderValue: Joi.number().integer(),
    maxCoupanAmount: Joi.number().integer(),
    coupenPercentage: Joi.number().integer(),
    coupanCode: Joi.string(),
    productId: Joi.array().items(Joi.objectId()),
    storeId: Joi.array().items(Joi.objectId()),
  }),
  params: Joi.object().keys({
    userCoupanId: Joi.objectId().required(),
  }),
};

export const getUserCoupanById = {
  params: Joi.object().keys({
    userCoupanId: Joi.objectId().required(),
  }),
};

export const deleteUserCoupanById = {
  params: Joi.object().keys({
    userCoupanId: Joi.objectId().required(),
  }),
};

export const getUserCoupan = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedUserCoupan = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
