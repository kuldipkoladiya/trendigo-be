/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createInventoryAddress = {
  body: Joi.object().keys({
    country: Joi.string(),
    city: Joi.string(),
    state: Joi.string(),
    pincode: Joi.number().integer(),
    street: Joi.string(),
    storeId: Joi.objectId(),
    inventoryId: Joi.objectId(),
  }),
};

export const updateInventoryAddress = {
  body: Joi.object().keys({
    country: Joi.string(),
    city: Joi.string(),
    state: Joi.string(),
    pincode: Joi.number().integer(),
    street: Joi.string(),
    storeId: Joi.objectId(),
  }),
  params: Joi.object().keys({
    inventoryAddressId: Joi.objectId().required(),
  }),
};

export const getInventoryAddressById = {
  params: Joi.object().keys({
    inventoryAddressId: Joi.objectId().required(),
  }),
};

export const deleteInventoryAddressById = {
  params: Joi.object().keys({
    inventoryAddressId: Joi.objectId().required(),
  }),
};

export const getInventoryAddress = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedInventoryAddress = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
