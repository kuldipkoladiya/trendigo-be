/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createInventory = {
  body: Joi.object().keys({
    store: Joi.objectId(),
    name: Joi.string(),
    sourceType: Joi.string(),
    productTitle: Joi.string(),
    sourceAddressId: Joi.objectId(),
    contactName: Joi.string(),
    email: Joi.string().email(),
    mobileNumber: Joi.number().integer(),
    countryCode: Joi.string(),
    fax: Joi.string(),
    latitude: Joi.object().unknown(true),
    longitude: Joi.object().unknown(true),
    priority: Joi.number().integer(),
    status: Joi.string(),
  }),
};

export const updateInventory = {
  body: Joi.object().keys({
    store: Joi.objectId(),
    name: Joi.string(),
    sourceType: Joi.string(),
    productTitle: Joi.string(),
    sourceAddressId: Joi.objectId(),
    contactName: Joi.string(),
    email: Joi.string().email(),
    mobileNumber: Joi.number().integer(),
    countryCode: Joi.string(),
    fax: Joi.string(),
    latitude: Joi.object().unknown(true),
    longitude: Joi.object().unknown(true),
    priority: Joi.number().integer(),
    status: Joi.string(),
  }),
  params: Joi.object().keys({
    inventoryId: Joi.objectId().required(),
  }),
};

export const getInventoryById = {
  params: Joi.object().keys({
    inventoryId: Joi.objectId().required(),
  }),
};

export const deleteInventoryById = {
  params: Joi.object().keys({
    inventoryId: Joi.objectId().required(),
  }),
};

export const getInventory = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedInventory = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};
