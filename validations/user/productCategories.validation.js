/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

export const createProductCategories = {
  body: Joi.object().keys({
    value: Joi.string().required(),
    isSubCategory: Joi.bool(),
    parentCategoryId: Joi.string(),
  }),
};

export const updateProductCategories = {
  body: Joi.object().keys({
    value: Joi.string(),
    isSubCategory: Joi.bool(),
    parentCategoryId: Joi.string(),
  }),
  params: Joi.object().keys({
    productCategoriesId: Joi.objectId().required(),
  }),
};

export const getProductCategoriesById = {
  params: Joi.object().keys({
    productCategoriesId: Joi.objectId().required(),
  }),
};

export const deleteProductCategoriesById = {
  params: Joi.object().keys({
    productCategoriesId: Joi.objectId().required(),
  }),
};

export const getProductCategories = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number(),
      limit: Joi.number(),
    })
    .unknown(true),
};

export const paginatedProductCategories = {
  body: Joi.object().keys({}).unknown(true),
  query: Joi.object()
    .keys({
      page: Joi.number().default(1),
      limit: Joi.number().default(10).max(100),
    })
    .unknown(true),
};

export const getProductparentCategoryId = {
  params: Joi.object().keys({
    parentCategoryId: Joi.objectId().required(),
  }),
};
